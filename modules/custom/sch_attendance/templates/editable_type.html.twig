{#
/**
 * @file editable_type.html.twig
 * Default theme implementation to present Editable_type data.
 *
 * This template is used when viewing Editable_type pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_editable_type()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('editable_type') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
